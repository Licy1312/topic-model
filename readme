# 系统结构说明
topic-dao:处理数据库模块，DML控制层模块
topic-service：逻辑层，事务控制模块
topic-analyzer：分词器，对分本预处理、分词处理模块
topic-lda：LDA模型生成模块
topic-sort：文本排序算法实现模块
topic-web：数据组织、封装，页面展示模块

# 开发说明
1.所有的单元测试，写到topic-文本成对应的包下面。
2.所有的类、方法简单都要描述一下功能。
3.每一次修改代码，需要从master分支切换到自己的分支，然后提交到远程服务器上，本地不要合并代码。
4.每一次提交代码一定要写提交说明。
5.本地更新了数据库，提交代码的时候要将新的数据库导出放到Database目录下，命名规则一致，
  并且在提交代码的时说明数据库修改的那些地方。